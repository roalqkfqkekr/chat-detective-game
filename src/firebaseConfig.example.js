// 1. Copy this file to `src/firebaseConfig.js`.
// 2. Paste the Firebase web app config copied from Firebase Console.
// 3. Do NOT commit `src/firebaseConfig.js`; it is ignored by .gitignore.
//
// Firebase AI Logic with the Gemini Developer API can run without Firestore,
// Storage, Auth, Cloud Functions, Express, or a separate backend for this MVP.

export const firebaseConfig = {
  apiKey: "YOUR_FIREBASE_WEB_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_FIREBASE_APP_ID",
};
